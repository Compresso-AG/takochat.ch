# Tako – takochat.ch

RewriteEngine On

# www.takochat.ch -> https://takochat.ch (ohne www, 301 permanent)
# Das Ziel ist immer https; http->https erzwingt die Plattform bereits selbst.
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]
